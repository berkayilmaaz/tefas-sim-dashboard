[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tefas-sim-dashboard"
version = "0.1.0"
description = "TEFAS strategy simulator"
readme = "READ.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
